		# Access built-in EuStockMarkets data
		data("EuStockMarkets")
		
		# Select one market for analysis, e.g., DAX
		dax <- EuStockMarkets[, "DAX"]
		
		# Calculate log returns of DAX
		dax_returns <- diff(log(dax))
		
		# Remove the first NA value caused by diff()
		dax_returns <- na.omit(dax_returns)
		
		# Calculate the LRV 
		lrv_result<- sandwich::lrvar(dax_returns, type = "Newey-West")* ength(dax_returns) 
